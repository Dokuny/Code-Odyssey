import { colors } from '../../../../config/Color';
import { Body1 } from '../../../atoms/basic/Typography';

const MusicBot = () => {
  return (
    <>
      <Body1 children={'MusicBot'} color={colors.Gray[300]} />
    </>
  );
};

export default MusicBot;
