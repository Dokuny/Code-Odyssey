import tokenInstance from '../../axios/tokenInstance';

// export const getPersonalProfile = async (memberId: string) => {
//   const data = await tokenInstance.get(`보낼곳`, { memberId });
//   return data && data.data;
// };
